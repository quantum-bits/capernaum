\documentclass{article}

\usepackage[margin=0.75in]{geometry}
\usepackage{graphicx}
\usepackage{multicol}
\usepackage[colorlinks]{hyperref}
\usepackage{framed}

\usepackage{pgfplots}
\pgfplotsset{compat=1.16}

\begin{document}

{% for elt in letter.letterElements %}
  {% if elt.letterElementType.key == "boilerplate" %}
    <$ elt.renderBoilerplate() $>
  {% elif elt.letterElementType.key == "boolean-calculation-results" %}
    {% include "se-results.tex" %}
  {% elif elt.letterElementType.key == "footer" %}
    {% include "footer.tex" %}
  {% elif elt.letterElementType.key == "header" %}
    {% include "header.tex" %}
  {% elif elt.letterElementType.key == "chart" %}
    {% include "chart.tex" %}
  {% elif elt.letterElementType.key == "image" %}
    IMAGE <$ elt.renderImage() $> END IMAGE
  {% else %}
    Don't know how to render <$ elt.letterElementType.key $>.
  {% endif %}
{% endfor %}

\end{document}
